#!/bin/bash
# ABOUTME: Per-challenge cleanup. Nothing long-running here, but drop the test
# results so the next chapter starts from a clean tree.
set -euo pipefail

rm -rf /root/workshop/build/test-results /root/workshop/build/reports 2>/dev/null || true

echo "Exercise 6 cleanup: OK"
